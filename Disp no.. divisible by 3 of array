#include<stdio.h>
int main()
{
        int i,n,a[100];
        printf("enter limit:");
        scanf("%d",&n);
        printf("enter n numbers:");
        for(i=0;i<n;i++)
        {
        scanf("%d",&a[i]);
        }
        for(i=0;i<n;i++)
        {
        if(a[i]%3==0)

        printf("\n number divisible by 3=%d\t",a[i]);
        }
}
